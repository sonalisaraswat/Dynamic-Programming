#include <stdio.h>
int main(){
int N,M,A[10],i,j;
scanf("%d",&N);
for (i=0;i<N;i++){
    scanf("%d",&A[i]);
}
scanf("%d",&M);
int B[M+1];
B[0] = 1;
for(j=1;j<=M;j++){
    if(j%A[0]==0){
        B[j] = 1;
    }
    else{
        B[j] = 0;
    }
}
for(i=1;i<N;i++){
    for(j=0;j<=M;j++){
        if(j>=A[i]){
            B[j] = B[j] + B[j-A[i]];
        }
    }
}
printf("%d",B[M]);
return 0;
}
